<div class="navbar navbar-default navbar-top sit-on-top" role="navigation">
  <div class="container top-header">
    <div class="navbar-header">
      {{# link-to 'home' class='navbar-brand' }}
      <!-- <img class="brand-logo img-dp-shadow" src="assets/images/sdg-logo-only70x70.png"> -->
      <span class="glyphicon glyphicon-list-alt" style="font-size: 55px;color: #005E95;"></span>
      {{/ link-to }}
      <div class='navbar-brand'>
      {{# link-to 'home' class='navbar-brand' }}
        <h4>{{ t 'header.title' }}</h4>
      {{/ link-to }}
      </div>
    </div>
    <div class='collapse navbar-collapse'>
      <ul class='nav navbar-nav navbar-right pad-top-10'>
        <ul class='nav navbar-nav navbar-right'>
          {{ago-signin currentUrl=applicationController.currentAppUrl showSettings=(action "showSettings") }}
        </ul>
      </ul>
    </div>
  </div>
</div>

{{outlet}}
